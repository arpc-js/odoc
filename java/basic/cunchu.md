# 计算机=计算+存储
昨天讲的计算，今天讲存储

## 计算机存储
- 用电存储代替人脑存
- 网吧讲到量子隧穿了，然后继续
## 闪电->电存->量子隧穿持久化->文件->mysql内核
## 别人以为初中毕业技术很菜其实是天花板级别
## 人心中的成见是一座大山
给电子施加电场力，能穿过绝缘层，出不来
- 思考如何用量子隧穿存1位数据01？
- 构造2种状态，电子囚禁就是1，没有电子就是0
- 8个这样的就是8位，256=1字节->kb->mb->gb->tb
- 意义:双态电路存储无法持久化，目前可以持久化
- 指令->封装接口抽象->文件
- java/go读写文件io，
- 写入txt能持久化存储数据
- 无法查询
- 怎么样子能查询txt文件里面的内容
- 拆分每一行数据，
- 每一行数据有大小
- age int64，score int64，status int1->一行大小
- 这一行数据=129位
- 第二行258位
- 你要查id=2，前提:要知道258偏移量
- 然后文件偏移258就是第二行
- 没有id到偏移量的映射
- 所有行数扫描，每隔129位扫描一次（全表扫描）
- id->偏移量映射，
- 有映射之后拿到id->offset->数据
- 索引：id到偏移量的映射
- 查询id=1000的数据怎么查？
- 1.很多个id中找到id=1000
- 用一个容器存很多id，然后查询id
## 数组/连表存很多个id，然后查询id
- O(n)=全表扫描，此时映射索引系统没意义
- 2.id映射offset偏移，找到数据

## 二叉树映射系统存很多个id，然后查询id
- 1.n->logn查到id
- 2.id映射offset偏移，找到数据
- 缺点:可能退化链表
- 我们先找到id(数组/树(二叉平衡/多叉平衡)/hash)，在磁盘偏移映射
- 数据库内核
- 事务:先插入日志
- undo log，redo log
- 模拟事务:插入一半报错了?数据不一致了，
- undo log查看，撤销
## sql where是如何实现？
- 单条件:先走查询系统(平衡树/hash)，
- 然后磁盘偏移映射
- 多条件:拆分单条件
- 一个一个走，映射，查询
- 逻辑判断短路(防止无用执行)
- 逻辑运算 and or->交集，并集
- 返回交集并集



# 明年路线:
- 1.出跨语言的编程基础教程(计算+存储+通信)
前端(vue+小程序+桌面移动)，后端(java.nodejs,go python)
- 跨语言国际化出海
- 讲架构/底层:架构/计算+存储+通信

# OS之上vm->软件
## 软件架构
- 1.电报/电话,无图像
- 2.静态网页，文字渲染图像
- 3.动态网页:
不同请求服务器，每次返回新html，
- 无刷新技术:每次请求返回需要的
html片段，只刷新一个片段
- htmx->无感知片段刷新:构建html片段麻烦
- 前后端分离:后端返回数据，前端js html水合
操作dom渲染后端数据
- 单页前后端分类架构:url拦截path，js切换
对应的html片段，组件化
- vue.js：Proxy代理数据跟新自动更新dom
- vue缺陷是什么？水合慢，先获取html，获取js
然后水合html，js，渲染data->dom
- 首次url去掉水合和渲染，那么不就能优化vue了吗？怎么做了？
非首次不需要去掉
- 请求url，返回不需要渲染的单页，
前端url劫持path->跳路由页->
js获取到这个js组件对象虚拟dom
js dom渲染这个组件对象，单页就显示对应页面

那我们直接把vue首次url在服务端劫持path
服务端path获取虚拟dom对象
此时虚拟dom就是url对应的页面组件
js是不是可以在服务端执行？
那服务端直接通过main.js渲染vue对应url的首页
url对应的无水合，无dom渲染的页面就有了
然后把这个页面返回给前端瞬间显示，
- 但是：无法交互
main.js再次拿到前端，然后水合给html
看见页面，前端偷偷水合其他页

- 同构ssr架构:
- js能写同构:一套代码，在服务端客户端直接无缝切换
- 同一套代码，又是后端，又是前端？这怎么写？
- 同构ssr架构:网站课程页面，直接打开是后端sql渲染，
通过菜单点击，那么数据又由fetch/ajax获取？一套js如何实现？
- rpc同构:一个对象在服务端是直连数据库，在客户端是fetch rpc
- 为什么要用同构ssr做网站？
- 1.vue csr首次慢，无seo
- ssr缺点:虽然能无感片段刷新，但是慢
同构ssr:同一套代码首次是ssr，之后是水合csr
解决了首次慢和之后慢的问题

- 操作数据库
- 工资没意义，上班限制了我，项目没做完，不好走
我去卖课千万应该是没问题

- 听我的课随便一个片段
- 都是拨云见日，不开玩笑

# curd如何进步：
高级程序员的界限是io，
不是微服务，分布式，八股文
- io流:存储的核心
- 存储是计算机的半壁江山，计算是另一半
- 普通io/nio
- 异步，状态机，栈的操作，切换栈pc跳转
- 状态机:能然你并发非常牛逼
- 虚拟线程:有栈协程，并发非常强
- 状态机并发>java 虚拟线程的
  curd是存储，io是存储的入口
- crud就是存储啊，io学牛逼，
你就能很牛逼存储了，技术就非常牛了

-- nio，状态机，netty模型
 协议，可以优化带宽，数据量小传输快

- 目前并发模型：
- 有栈协程：go,java虚拟线程
- rust,c#,状态机，无栈协程

# coroutine协程
- 1.有栈:coroutine对象里面放了栈和sp指针
io操作当初协程上下文，io结束恢复上下文
- 2.无栈:状态标记，什么时候执行什么状态
- 
new coroutine(fn)
coroutine里有sp栈指针，stack，fn协函数
go function(){
} 创建协程对象，协程对象里放函数，sp，栈
把协程对象加入到调度队列